@extends('layouts.app')
@section('content')<div class="container"><div class="d-flex justify-content-between"><h1>Quản lý phim</h1><a class="btn btn-primary" href="{{ route('admin.movies.create') }}">Thêm phim</a></div>@if(session('status'))<div class="alert alert-success">{{ session('status') }}</div>@endif<table class="table"><thead><tr><th>Phim</th><th>Loại</th><th>Thể loại</th><th>Tập</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($movies as $movie)<tr><td>{{ $movie->movie_name }}</td><td>{{ $movie->type }}</td><td>{{ optional($movie->genre)->genre_name }}</td><td>{{ $movie->episodes->count() }}</td><td>{{ $movie->status?'Hiện':'Ẩn' }}</td><td><a href="{{ route('admin.movies.edit',$movie) }}">Sửa</a><form class="d-inline" method="POST" action="{{ route('admin.movies.destroy',$movie) }}" onsubmit="return confirm('Xóa phim và toàn bộ tập?')">@csrf @method('DELETE') <button class="btn btn-link text-danger">Xóa</button></form></td></tr>@empty<tr><td colspan="6">Chưa có phim.</td></tr>@endforelse</tbody></table>{{ $movies->links() }}</div>@endsection
