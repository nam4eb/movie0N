@extends('layout')
@section('title','Xem '.$movie->movie_name.' - Tập '.$episode->eps_num)
@section('content')<div class="container py-4 text-white"><h1>{{ $movie->movie_name }} <small>— Tập {{ $episode->eps_num }}</small></h1><div class="embed-responsive embed-responsive-16by9 bg-dark"><iframe class="embed-responsive-item" src="{{ $episode->link }}" title="{{ $movie->movie_name }} tập {{ $episode->eps_num }}" allow="autoplay; fullscreen; picture-in-picture" allowfullscreen></iframe></div><div class="my-3"><strong>Danh sách tập:</strong> @foreach($episodes as $item)<a class="btn m-1 {{ $item->is($episode) ? 'btn-warning' : 'btn-outline-light' }}" href="{{ route('movies.watch',[$movie,$item]) }}">{{ $item->eps_num }}</a>@endforeach</div><p>{{ $movie->description }}</p><a href="{{ route('movies.show',$movie) }}">← Thông tin phim</a></div>@endsection
