@extends('layout')
@section('title', 'movieON - Xem phim trực tuyến')
@section('content')<div class="container-fluid px-md-5">@if($featured->isNotEmpty()) @php($hero=$featured->first()) <section class="movie-main-content rounded" style="background-image:linear-gradient(90deg,rgba(0,0,0,.9),rgba(0,0,0,.15)),url('{{ asset('img/'.($hero->backdrop ?: $hero->image)) }}')"><div class="p-5 col-md-7"><h1>{{ $hero->movie_name }}</h1><p>{{ $hero->description }}</p><a class="btn btn-light" href="{{ route('movies.show',$hero) }}">Xem ngay</a></div></section>@endif<section class="movie-list"><h2>PHIM MỚI CẬP NHẬT</h2><div class="row">@each('partials.movie-card',$latest,'movie','partials.empty')</div></section>@if($series->isNotEmpty())<section class="tv-series"><h2>PHIM BỘ</h2><div class="row">@each('partials.movie-card',$series,'movie')</div></section>@endif</div>@endsection
